<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}

LISTE DES ETUDIANTS</br>
<table>
    {% for instrument in pInstruments %}
        <tr>
            <tr>
                <td>Id : {{ instrument.id }}</td>
                <td>Nom : <a href="{{ path('instrumentConsulter', { 'id': instrument.id }) }}">{{ instrument.nom }}</a></td>
                <td>Marque de l'instrument : {{ instrument.idmarque.libelle }}</td>
                <td>Type de l'instrument : {{ instrument.idtype.libelle }}</td>
                <td>Classe de l'instrument : {{ instrument.idtype.idclasse.libelle }}</td>
                <td>Numéro de série de l'instrument : {{ instrument.numSerie }}</td>
                <td>Date d'achat de l'instrument : {{ instrument.dateAchat|date("d/m/Y") }}</td>
                <td>Prix d'achat de l'instrument : {{ instrument.prixAchat }}</td>
                <td>Utilisation de l'instrument : {{ instrument.utilisation }}</td>
                <td></td>
            </tr>
    {% else %}
        <tr>
            <td>Aucun etudiant n'a été trouvé.</td>
        </tr>
    {% endfor %}
</table>


</body>
</html>