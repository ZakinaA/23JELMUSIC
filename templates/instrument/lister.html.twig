<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}

LISTE DES INSTRUMENTS</br>
<table border="2px" style="text-align: center">

    <tr>
        <td>ID</td>
        <td>NOM</td>
        <td>MARQUE</td>
        <td>TYPE</td>
        <td>CLASSE</td>
        <td>NUM SERIE</td>
        <td>DATE D'ACHAT</td>
        <td>PRIX D'ACHAT</td>
        <td>UTILISATION</td>
    </tr>
    
    {% for instrument in pInstruments %}

        <tr>
            <td>{{ instrument.id }}</td>
            <td><a href="{{ path('instrumentConsulter', { 'id': instrument.id }) }}">{{ instrument.nom }}</a></td>
            <td>{{ instrument.marque.libelle }}</td>
            <td>{{ instrument.type.libelle }}</td>
            <td>{{ instrument.type.classe.libelle }}</td>
            <td>{{ instrument.numSerie }}</td>
            <td>{{ instrument.dateAchat|date("d/m/Y") }}</td>
            <td>{{ instrument.prixAchat }} €</td>
            <td>{{ instrument.utilisation }}</td>
        </tr>
    {% else %}
        <tr>
            <td>Aucun etudiant n'a été trouvé.</td>
        </tr>
    {% endfor %}
</table>


</body>
</html>