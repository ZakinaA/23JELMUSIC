<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}LISTE DES COURS{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>
{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}

<h5>LISTE DES COURS :</h5>
<table>
    {% for c in pCours %}
        <tr>
            <td>Jours du cours</td>
            <td>Heure de début</td>
            <td>Heure de fin</td>
            <td>Libelle du cours</td>
            <td>Âge minimum</td>
            <td>Nombre de place dans le cours</td>
        </tr>
        <tr>
        <td>{{c.jours.libelle}}</td>
        <td>{{c.heuredebut|date('H:i:s')}}</td>
        <td>{{c.heurefin|date('H:i:s')}}</td>
        <td><a href="{{ path('coursConsulter', { 'id': c.id }) }}">{{ c.libelle }}</a></td>
        <td>{{c.agemini}}</a></td>
        <td>{{c.nbplaces}}</td>
    {% else %}
        <tr>
            <td>Aucun cours n'a été trouvé.</td>
        </tr>
    {% endfor %}
</table>
</body>
</html>