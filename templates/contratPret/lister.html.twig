<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
</head>
<body>

<h2>LISTE DES CONTRATS DE PRÊT</h2>

<table border="1">
    <tr>
        <th>instrument</th>
        <th>Date Début</th>
        <th>Date Fin</th>
        <th>Nom élève</th>
        <th>Prénom élève</th>

    </tr>

    {% for c in pContratPret %}
        <tr>
            {% if c.eleve is defined and c.instrument is defined %}

                <td>{{ c.instrument.nom }}</td>

            {% else %}
                <td>N/A</td>
                <td>N/A</td>
            {% endif %}

                <td><a href="{{ path('ContratPretConsulter', { 'id': c.id }) }}">{{ c.dateDebut|date('d/m/Y') }}</td>
                <td>{{ c.dateFin|date('d/m/Y')}}</td>

            {% if c.eleve is defined%}

                <td>{{ c.eleve.nom }}</td>
                <td>{{ c.eleve.prenom }}</td>

            {% else %}
                <td>N/A</td>
                <td>N/A</td>
            {% endif %}
        </tr>
    {% endfor %}

    {% if pContratPret is empty %}
        <tr>
            <td colspan="8">Aucun contrat de prêt n'a été trouvé.</td>
        </tr>
    {% endif %}
</table>

{% block javascripts %}{% endblock %}

</body>
</html>

