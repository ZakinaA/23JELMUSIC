{% extends 'base.html.twig' %}

{% block title %}Ajouter un prêt{% endblock %}
{% block body %}
        <div class="card" style="box-shadow: lightgrey 1px 1px 10px 1px">
            <div class="card-body">
                <h5 class="card-title fw-semibold mb-4">Ajouter un prêt</h5>
                {# templates/etudiant/ajouter.html.twig #}
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
                <a href="/23JELMUSIC/public/contratPret/lister"><button type="button" class="btn btn-primary m-1">Retour</button></a>

            </div>
       </div>
{% endblock %}